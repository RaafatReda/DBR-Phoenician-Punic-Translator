import React from 'react';

// FIX: This component was unused and causing build errors due to missing dependencies.
// The implementation has been removed to resolve these errors.
const DailyWord: React.FC = () => {
  return null;
};

export default DailyWord;
